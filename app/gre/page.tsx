import { GREView } from "@/components/gre/gre-view";

export const metadata = {
  title: "GRE Team | CIPACA AI",
  description: "Guest Relations Executive team management",
};

export default function GREPage() {
  return <GREView />;
}
